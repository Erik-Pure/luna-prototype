"use client";

import { Typography } from "@mui/material";
import styles from "../../page.module.scss";

export function FreightTab() {
  return (
    <div className={styles.contractTabPlaceholder}>
      <Typography className={styles.contractInfoValue}>Frakt - innehållsvy för prototyp.</Typography>
    </div>
  );
}
